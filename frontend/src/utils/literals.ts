export interface Literals {
    filterHeading: string,
    allBooks: string,
    noBooksMessage: string,
    searchPromptMessage: string
}
const literals: Literals = {
    filterHeading: "Filter your results",
    allBooks: "All books",
    noBooksMessage: "No books availabe at the moment",
    searchPromptMessage: "Please enter a search query"
}

export default literals